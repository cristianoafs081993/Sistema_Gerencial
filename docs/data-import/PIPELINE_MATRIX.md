# PIPELINE_MATRIX

## Matriz resumida

| Dominio | Pagina de Destino / Visualização | Gatilho de Importação | Parser/Service | Destino | Observacoes |
|---|---|---|---|---|---|
| Financeiro | `Financeiro.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `financeiroImportService.ts` | `financeiro_fonte_vinculacao` | Consolida saldo por fonte e vinculacao |
| LC | `LC.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `lcImportService.ts` | `lc_credores` | Tambem compara PDFs e gera macro SIAFI |
| Descentralizacoes | `Descentralizacoes.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `ImportacaoDados.tsx` | `descentralizacoes` | Upload principal aceita `NC`, `NC Celula - Tipo` e `NC - Operacao (Tipo)`; resume a NC para `2026NC000001`; quando `NC Celula - Tipo` vier preenchido, rotulos que contenham `DESTINO` sempre entram positivos, rotulos que contenham `ORIGEM` so entram negativos em `ANULACAO DE DESCENTRALIZACAO DE CREDITO` e `ORIGEM` sem anulacao e ignorado; sem a coluna de tipo, preserva a regra historica e ignora a origem inferida quando uma descentralizacao normal vier pareada como `339000` mais outra natureza para a mesma NC/data/descricao/PTRES/PI/valor; `NC Celula - Plano Interno` continua separando linhas distintas da mesma NC e a tabela persiste `nota_credito` e `operacao_tipo` para reconciliacao |
| Devolucoes de descentralizacoes | `Descentralizacoes.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `descentralizacoesService.processDevolucao` | `descentralizacoes` | Fluxo separado do upload principal; insere uma nova linha negativa com `operacao_tipo = DEVOLUCAO`, nao reutiliza a logica de anulacao do CSV principal e pode receber valores negativos diretamente |
| Conta de descentralizacoes | `Descentralizacoes.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `descentralizacoesContaSaldosService.upsertBatch` | `descentralizacoes_conta_saldos` | Usa o relatorio agregado `PTRES / Metrica / Valor`; alguns exports reais deixam o valor na terceira coluna sem cabecalho e o parser precisa aceitar esse layout; serve apenas para os somatorios geral, por PTRES e por dimensao, sem substituir a grade detalhada de `descentralizacoes`; linhas sem `PTRES` ou `Valor` valido sao descartadas |
| PFs | `RastreabilidadePFs/index.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `pfImportService.ts` | `pf_solicitacao`, `pf_aprovacao`, `pf_liberacao`, `pf_fonte_recurso` | Usa dois arquivos correlacionados via `PFImportDialog` |
| Contratos locais | `Contratos.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `ContratosSyncDialog.tsx` + `contratos.ts` | `contratos`, `contratos_empenhos` | O relatorio de contratos ativos limpa a razao social removendo CNPJ e o sufixo `Pessoa Juridica`; contratos inativos no arquivo sao ignorados e contratos ausentes no arquivo nao devem ser removidos do banco; o relatorio de vinculos/valores atualiza o valor do contrato e os vinculos com empenhos, ignorando `Valor Liquidado` e `Valor Pago`; a persistencia deduplica por `numero` e usa fallback para `insert/update` quando o `upsert` nao e aceito pelo banco; por regra operacional, o contrato legado `00089/2016` da CAERN e ignorado no modulo local por ter sido substituido por contrato mais recente com o mesmo objeto |
| Retencoes FD-Reinf | `RetencoesFdReinfDesign.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `retencoesEfdReinfImportService.ts` | `retencoes_efd_reinf` | UI aceita XLSX, parser atual nao; alertas tratados podem ser marcados em `correcao_realizada` e deixam as pendencias abertas |
| Documentos habeis | `LiquidacoesPagamentos.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `transparencia.importDocumentosHabeis` | `documentos_habeis`, `documentos_habeis_itens`, `documentos_habeis_situacoes` | Usa `JsonImportDialog` |
| Liquidacoes / Fonte SOF | `LiquidacoesPagamentos.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `transparencia.importLiquidacoes` | `documentos_habeis` | Atualiza `fonte_sof` e vinculo com empenho |
| Ordens bancarias | `LiquidacoesPagamentos.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `transparencia.importOrdensBancarias` | `documentos_habeis_itens` e `documentos_habeis` | Atualiza pagos |
| Situacoes / Retencoes | `LiquidacoesPagamentos.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `retencoes.upsertSituacoesBatch` | `documentos_habeis_situacoes` | Regras de retencao derivadas da situacao |
| Saldos SIAFI / RAP | `Empenhos.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `parseSiafiCsv` + `syncSiafiDataToDb` | `empenhos` | Upload manual atualiza empenhos existentes e cria empenhos de exercicio ou RAP ausentes. O upload aceita dois contratos: CSV SIAFI de empenhos do exercicio e CSV especifico de saldo de RAP; detecta UTF-8, UTF-16LE, UTF-16BE e Latin-1. Quando o cabecalho vier com `NE CCor` + `Metrica`, o parser trata o arquivo como saldo de RAP e persiste `saldo_rap_oficial` diretamente sem zerar `rap_inscrito`, `rap_a_liquidar`, `rap_liquidado`, `rap_pago` ou `valor_liquidado_a_pagar` ja existentes. O CSV legado combinado com colunas de RAP continua sendo aceito e, nesse caso, ainda usa `RESTOS A PAGAR NAO PROCES. LIQUIDADOS A PAGAR` para alimentar `valor_liquidado_a_pagar` e ajustar o saldo operacional; o layout com virgulas e colunas `RESTOS A PAGAR ... (PROC E N PROC)` segue coberto por regressao em `siafiParser.test.ts` |
| Historico anual de RAP | `DashboardRapAnnualEvolutionPanel.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `parseRapHistoricoAnualFile` + `rapHistoricoAnualService.importReport` | `rap_historico_anual` | Pipeline para CSV agregado anual por UG e item de informacao. Localiza cabecalho apos linhas de titulo/filtro com `UG Executora`, `NE CCor - Ano Emissao`, `Metrica` e `Item Informacao`; aceita valor em coluna sem nome; ignora `Total`; grava novo lote por `import_batch_id` sem apagar historicos anteriores. A aba RAP do dashboard le o ultimo lote, filtra por UG e exibe barras empilhadas dos itens `35`, `40`, `41` e linha de total do item `50`, com fallback para soma dos componentes quando o item `50` nao existir |
| Creditos disponiveis | `CreditoDisponivel.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `src/services/creditosDisponiveisDetalhes.ts` + `src/utils/creditosDisponiveisDetalhes.ts` | `creditos_disponiveis_detalhes` + `creditos_disponiveis` | Exibe o snapshot detalhado por PI mais recente; aceita descricao e valor sem cabecalho e atualiza o agregado legado por PTRES |
| Energia Campus | `pages/energia/EnergiaCampus.tsx` | `ImportacaoDados.tsx` (`/importacao-dados`) | `energiaCampusService.ts` | `energia_import_runs`, `energia_consumo_faturas`, `energia_solar_geracao`, `energia_contratos`, `energia_contrato_execucoes` | Importa a planilha `Levantamento de Consumo - COSERN.xlsx`; consome abas `Consumo`, `Previsão - Mercatto`, `UFV's`, `Valor Executado` e abas anuais. Mercatto usa kWh real quando a base traz essa informação; quando só houver fatura/liquidação de contratos API, a Visão Geral pode estimar kWh pela tarifa média conhecida, sem persistir a estimativa. |
| Ingestao automatica por Gmail | `Gmail + Apps Script` | Automático por E-mail | `ingest-email-csv` | `email_csv_ingestion_runs` + tabela final do pipeline | Roteia anexos CSV para os parsers server-side; exige `EMAIL_CSV_INGEST_SECRET`; suporta todos os pipelines CSV do SIAFI (incluindo conta agregada de descentralizacoes em `descentralizacoes_conta_saldos`, restos a pagar/NE-RO em `empenhos`, `ordens_bancarias` e PFs via `pf_solicitacoes` e `pf_aprovacoes` com reconciliação automática) |
| Pesquisa de preços | `PesquisaPrecos.tsx` | `PesquisaPrecos.tsx` | `priceResearch.ts` + `priceCatalog.ts` + `pesquisar-precos` | `price_researches`, `price_research_items` | Exige descrição, quantidade e unidade; CATMAT/CATSER pode ser sugerido pelos catálogos compactados locais quando ausente e sempre exige confirmação humana; PDFs escaneados ainda não têm OCR; consulta até 100 preços homologados dos últimos 12 meses no Compras.gov.br |

## Dialogos e componentes reutilizados

- importacao generica: [JsonImportDialog.tsx](/C:/Users/crist/OneDrive/Desktop/Obsidian/01%20-%20Projetos/Apps/Sistema_Gerencial/src/components/JsonImportDialog.tsx)
- importacao de PFs: [PFImportDialog.tsx](/C:/Users/crist/OneDrive/Desktop/Obsidian/01%20-%20Projetos/Apps/Sistema_Gerencial/src/components/modals/PFImportDialog.tsx)
- sincronizacao de contratos: [ContratosSyncDialog.tsx](/C:/Users/crist/OneDrive/Desktop/Obsidian/01%20-%20Projetos/Apps/Sistema_Gerencial/src/components/modals/ContratosSyncDialog.tsx)
- parser utilitario de CSV: [csvParser.ts](/C:/Users/crist/OneDrive/Desktop/Obsidian/01%20-%20Projetos/Apps/Sistema_Gerencial/src/utils/csvParser.ts)

## Alertas de consistencia

- A UI de FD-Reinf aceita `.xlsx,.xls`, mas o service atual lanca erro para XLSX.
- O dialogo de PF sugere principalmente XLSX/XLS, mas o service tambem trata CSV.
- A sincronizacao de contratos depende de duas planilhas XLSX distintas: `Relatorio.xlsx` para contratos ativos e `Relatorio (1).xlsx` para valor do contrato e vinculos com empenhos. O dialogo identifica automaticamente a ordem correta pelos cabecalhos.
- O parser do `Relatorio (3)` ignora `Valor Liquidado` e `Valor Pago`; esses campos nao devem alimentar o modulo local de contratos.
- A escrita no banco pode cair para `insert/update` se o `upsert onConflict(numero)` nao estiver disponivel no ambiente.
- Alguns imports operam com JSON e CSV no mesmo fluxo porque usam `JsonImportDialog`.
- Na aba Restos a Pagar, o filtro `Pendente` usa saldo RAP operacional estritamente maior que zero; empenhos com saldo zerado por pagamento ou cancelamento ficam fora dele.


